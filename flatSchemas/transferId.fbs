namespace flats;

enum RequestId:byte {
    None = 0,
    Login = 10,
    JoinRoom = 11,
    LeaveRoom = 12,
}

enum ResponseId:byte {
    None = 0,
    Login = 10,
    JoinRoom = 11,
    MatchMaking = 12,
    MySpawnData = 13,     
}