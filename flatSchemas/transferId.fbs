namespace flats;

enum RequestId:byte {
    None = 0,
    Login = 10,
    JoinRoom = 11,
    MySpawnData = 12,
}

enum ResponseId:byte {
    None = 0,
    Login = 10,
    JoinRoom = 11,
    MySpawnData = 12,     
}